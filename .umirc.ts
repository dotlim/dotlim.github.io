import { defineConfig } from 'dumi';

export default defineConfig({
  title: 'dotlim',
  mode: 'site',
  navs: [
    null,
    {
      title: 'GitHub',
      path: 'https://github.com/dotlim',
    },
  ],
  exportStatic: {},
});
